<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.feedcomment.FeedCommentMapper">


    <select id="findAllByFeedIdLimitedTo">
        SELECT  c.feed_comment_id ,c.comment
                ,u.user_id AS writerUserId, u.nick_name AS writerNickName,
                 u.pic as writerPic , u.uid AS writerUid
        FROM feed_comment AS c
        JOIN USER  AS u  on u.user_id = c.user_id
        WHERE  c.feed_id = #{feedId}
        LIMIT #{startIdx} , #{sizePlusOne}

    </select>





</mapper>